@{
    ViewBag.Title = "Index";
}
@(Html.Kendo().TabStrip()
              .Name("tabstrip")
              .Items(tabstrip =>
              {
                  tabstrip.Add().Text("Books")
                      .Selected(true)
                      .LoadContentFrom(Url.Action("Grid", "Book"));

                  tabstrip.Add().Text("Authors")
                      .LoadContentFrom(Url.Action("Grid", "Author"));
              })
)